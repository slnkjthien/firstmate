project main before teardown : approved local delivery
origin  main before teardown : C1 upstream moved on

$ bin/fm-teardown.sh task-x1   (mode=local-only)
  teardown task-x1 complete (window firstmate:fm-task-x1, worktree /tmp/fm-teardown-tests.cj6bpH/local-only-teardown-refresh/wt)
  Backlog: task-x1 is closed in /tmp/fm-teardown-tests.cj6bpH/local-only-teardown-refresh/data/backlog.md. Run bin/fm-tasks-axi.sh ready for dependency-cleared candidates, check date gates, and dispatch only work whose blockers are gone and date is due.

project main after teardown  : approved local delivery
clone's origin/main after     : origin baseline
=> teardown refreshed the clone? no - it still holds the local landing, as documented

$ FM_HOME=<case> bin/fm-fleet-sync.sh project   (the next session start)
  project: STUCK: on diverged main, 1 commits behind origin/main - needs attention
project main after session start : approved local delivery
